{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "Run pytest on all tests",
			"command": "pytest --maxfail=50 --disable-warnings --tb=short --capture=tee-sys > tests_result.log 2>&1",
			"group": "test",
			"isBackground": false,
			"problemMatcher": [
				"$pytest"
			]
		}
	]
}